<div class="card col-md-5 col-12 shadow-sm p-4" style="min-width:300px; min-height: 700px">
	{% if project.media is not null and project.media.thumbnail is not null %}
		<div class="overflow-hidden justify-content-center" style="width: 100%; height: 300px;">
			<img src="/images/users/{{ project.media.thumbnail }}" class="card-img-top img-thumbnail mb-2" alt="{{ project.title }}">
		</div>
	{% else %}
		<div class="placeholder" style="width: 100%; height: 200px; background-color: #6c757d;"></div>
	{% endif %}
	<div class="card-body">
		<div class="d-flex mb-3 gap-1">
			{% if project.skill is not empty %}
				{% for skill in project.skill %}
					<span class="badge text-bg-secondary">
						<i class="fa-solid fa-code mr-1"></i>
						{{ skill.title }}</span>
				{% endfor %}
			{% endif %}
		</div>
		<h5 class="card-title">{{ project.title }}</h5>
		<p class="card-text">{{ project.description }}</p>
	</div>
	<div class="card-footer d-flex justify-content-between">
		<div>
			<button class="btn btn-primary shadow-sm" data-bs-toggle="modal" data-bs-target="#projectModal{{ project.id }}">Show details</button>
		</div>
		<div>
			{% if project.link is not empty %}
				<a href="{{project.link}}" type="button" class="btn btn-success shadow-sm">
					<i class="fa-solid fa-link"></i>
				</a>
			{% endif %}

			{% if project.githubLink is not empty %}
				<a href="{{project.githubLink}}" type="button" class="btn btn-success shadow-sm">
					<i class="fa-brands fa-github"></i>
				</a>
			{% endif %}
		</div>
	</div>
</div>
