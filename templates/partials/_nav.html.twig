<nav id="navbar" class="navbar navbar-expand-lg fixed-top navbar-dark bg-primary shadow-sm">
	<div class="container-fluid container">
		<a class="navbar-brand" href="{{ path('home')}}">
			<i class="fa-solid fa-briefcase mr-2"></i>
			PortFolio Creator
			{% if app.user %}
				/ Admin
				{% else %}

			{% endif %}
		</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor03" aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon "></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarColor03">
			<ul class="navbar-nav ms-auto">

				<li class="nav-item">
					<a class="nav-link " href="{{path('users')}}">Users</a>
				</li>

				{% if app.user %}
					<li class="nav-item">
						<a class="nav-link " href="{{path('admin.experience.index')}}">Experiences</a>
					</li>
					<li class="nav-item">
						<a class="nav-link " href="{{path('admin.project.index')}}">Projects</a>
					</li>

					<li class="nav-item">
						<a class="nav-link " href="{{path('admin.skill.index')}}">Skills</a>
					</li>

					<li class="nav-item">
						<a class="nav-link " href="{{path('admin.user.index',{id:app.user.id})}}">Informations</a>
					</li>

					<div class="ml-2 dropdown">
						<button class="btn btn-primary dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
							<i class="fa-solid fa-user"></i>
						</button>
						<ul class="dropdown-menu mt-2">
							<li>
								<a class="dropdown-item" href="{{path('admin.user.password',{id:app.user.id})}}">Edit password</a>
							</li>
							<li>
								<a class="dropdown-item" href="{{path('admin.user.delete',{id:app.user.id})}}">Delete account</a>
							</li>
							<li>
								<a class="dropdown-item" href="{{path('logout')}}">Logout</a>
							</li>
						</ul>
					</div>
				{% else %}
					<li class="nav-item">
						<a class="nav-link " href="{{path('login')}}">Login</a>
					</li>
					<li class="nav-item">
						<a class="nav-link " href="{{path('register')}}">Sign up</a>
					</li>
				{% endif %}

			</ul>
		</div>
	</div>
</nav>
