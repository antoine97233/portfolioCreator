{% extends 'base.html.twig' %}

{% block title %}Admin -
	{{ action }}
	{{ table }}
{% endblock %}

{% block body %}

	{% include 'partials/flash.html.twig' %}


	<div class="p-5">
		<h1 class="text-center p-4">{{ action }}
			{{ table }}</h1>

		{{form(form)}}
		</div>
	{% endblock %}
